<merge
	xmlns:android="http://schemas.android.com/apk/res/android">
	<ListView
		android:id="@+id/apps"
		android:background="@color/uikit_bg_ui"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:scrollbarStyle="outsideOverlay"
		android:clipToPadding="false"
		android:choiceMode="multipleChoice"
		android:divider="@color/uikit_separator"
		android:dividerHeight="1dp"/>
	<TextView
		android:id="@+id/no_hidden_apps"
		android:background="@color/uikit_bg_ui"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:gravity="center"
		android:padding="16dp"
		android:text="@string/no_hidden_apps"
		android:visibility="gone"/>
	<FrameLayout
		android:id="@+id/progress"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:background="@color/uikit_bg_ui"
		android:clickable="true"
		android:focusable="true"
		android:visibility="gone">
		<ProgressBar
			style="?android:attr/progressBarStyleLarge"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_gravity="center"
			android:gravity="center"/>
	</FrameLayout>
	<TextView
		style="@style/uikit_Toolbar"
		android:id="@+id/toolbar"
		android:drawableLeft="@drawable/uikit_ic_back"
		android:drawableStart="@drawable/uikit_ic_back"
		android:text="@string/hidden_apps" />
</merge>
